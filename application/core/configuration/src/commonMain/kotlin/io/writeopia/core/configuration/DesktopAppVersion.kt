package io.writeopia.core.configuration

/**
 * Desktop application version used when comparing against the latest published version.
 *
 * Keep this value aligned with the desktop distribution version when publishing a new release.
 */
object DesktopAppVersion {
    const val CURRENT = "0.47.0"
}
